#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0
 */
int main(void)
{
	int ls;

	for (ls = 'a'; ls <= 'z'; ch++)
	{
	if (ls != 'e' && ls != 'q')
		putchar(ls);
	}
	else
	{
	}
	}
	putchar('\n');
	return (0);
}
